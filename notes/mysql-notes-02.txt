
https://oreno-it3.info/archives/345


binary属性

データ型を宣言するとき、binaryオプションを指定することで、カラムの値を大文字と小文字を区別するようになります


unsigned属性

データ型を宣言するとき、UNSIGNEDオプションを指定することで、符号なしの範囲で値を扱うことが出来ます。unsigned属性の有無によって、値の範囲が次のように変化します。